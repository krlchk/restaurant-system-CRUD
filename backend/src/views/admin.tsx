import React = require("react");

export const Admin = () => {
  return (
    <html>
      <head>
        <title>Admin page</title>
      </head>
      <body>
        <h1>It admin's page!</h1>
      </body>
    </html>
  );
};
