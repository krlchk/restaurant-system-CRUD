<h2>Order #<%= order.id %>
</h2>
<p>Total: $<%= order.total_price %>
</p>
<p>Status: <%= order.status %>
</p>

<% if (order.status==='completed' ) { %>
    <form method="POST" action="/confirm-order/<%= order.id %>">
      </form>
    <% } %>

        <a href="/">Back to Orders</a>